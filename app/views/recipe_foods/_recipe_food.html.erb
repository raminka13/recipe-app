<tr>
  <td><%= recipe_food.id %><%= recipe_food.food.name %></td>
  <td><%= recipe_food.quantity %></td>
  <td><%= recipe_food.food.price %></td>
  <td><%= link_to "Edit this recipe food", edit_user_recipe_recipe_food_path(@user, @recipe, recipe_food) %> |
  <%= button_to "Destroy this recipe food", user_recipe_recipe_food_path(@user, @recipe, recipe_food), method: :delete %></td>
</tr>
